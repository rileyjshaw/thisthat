var TwitterStrategy = require('passport-twitter').Strategy;

var PASSPORT_OPTIONS = { passReqToCallback: true };

function twitterStrategies (db, passport) {
	// TODO(riley)
}

module.exports = twitterStrategies;
